# ChurchTools Suite v1.2.1.3

- Fix: CT delete sync now detects and removes ChurchTools events and appointments deleted during incremental sync.
- Improves sync consistency by merging Phase 1 and Phase 2 appointment key detection on incremental runs.
